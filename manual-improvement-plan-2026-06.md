# Cloud:iA 매뉴얼 개선 계획 — cloudia / cloudia-web 대비 갭 분석

> 작성일: 2026-06-08
> 대상 저장소: `CloudiA-manual`
> 대조 대상: `cloudia`(백엔드) · `cloudia-web`(웹 UI)

---

## 배경 (왜 작성하나)

`cloudia`(백엔드)와 `cloudia-web`(웹 UI)에 반영된 기능 대비, 사용자 매뉴얼에 **누락되었거나 갱신이 필요한 부분**을 식별하기 위한 문서입니다.

작성 원칙:

- **매뉴얼은 사용자 가이드입니다.** UI에 실제 화면·버튼·탭·폼으로 노출된 기능만 서술합니다. 백엔드 API만 있고 UI에 붙지 않은 기능은 대상에서 제외합니다.
- 표현은 **사용자 친화적**으로 작성합니다. (내부 구현 용어가 아닌, 화면에 보이는 라벨과 사용자가 하는 일 중심)
- 범위는 **관리자 + 프로젝트 매뉴얼 전체**입니다.
- **L7 로드밸런서**(리스너 편집/타겟 그룹/런타임 모니터링)는 **별도 담당자가 작성 중**이므로 이 계획에서 제외합니다.

식별 방법: 웹 UI 네비게이션·컴포넌트 ↔ 백엔드 도메인 ↔ 매뉴얼 페이지를 **3원 교차 대조**하고, 최근 커밋으로 이미 반영된 내용을 확인했습니다.

---

## 핵심 요약 (TL;DR)

**매뉴얼은 전반적으로 최신 상태입니다.** 저장소 내부 갭 트래커(`docs/steering/manual-gap-ui.md`)에 정리되어 있던 항목 대부분은 최근 커밋으로 **이미 해소**되었습니다. (예: VM 콘솔 접속, 사용자 스크립트 탭, 네트워크 토폴로지, 서브넷 DNS, 인스턴스 보안 유형/CPU 피닝/라이브 리사이즈, 물리 머신 BMC 탭 전부, 오브젝트 스토리지/VirtioFS, 쿠버네티스 CA 로테이션·kubeconfig·워커 노드그룹, 알림 SMTP/웹훅, LDAP 매퍼 등)

따라서 **실제로 새로 써야 할 작업은 소수**입니다.

| # | 항목 | 유형 | 우선순위 |
|---|------|------|----------|
| P1 | **내 정보(MyInfo) 페이지 신규 작성** | 신규 페이지 | 높음 |
| P2 | 인스턴스 상세 — `호스트 장치` / `AI 가속기` 탭 설명 보강 | 기존 페이지 보강 | 중간 |
| H | 내비게이션·용어 정합성 정리(묶음) | 하우스키핑 | 낮음 |

---

## 1. 실제 작업이 필요한 항목

### P1. 내 정보(MyInfo) 페이지 — **완전 누락 (신규 작성 필요)**

- **UI 근거**: `cloudia-web/src/pages/auth/myInfo/` (`MyInfo.tsx`, `MyInfoUpdate.tsx`, `MyInfoChangePasswordModal.tsx`). 로그인 후 **모든 사용자(관리자·프로젝트 멤버 공통)**가 우측 상단 계정 메뉴에서 접근하는 화면입니다.
- **현재 매뉴얼 상태**: 해당 화면을 설명하는 페이지가 **어디에도 없습니다.** `administrator/access-management/member/member.md`는 *관리자가 다른 사용자를 관리*하는 화면이라, 본인 프로필을 보고 수정하는 `내 정보`와는 다릅니다.
- **작성할 내용 (사용자 친화적 표현 예시)**:

  > **내 정보**에서는 로그인한 본인의 계정 정보를 확인하고, 연락처와 비밀번호를 직접 관리할 수 있습니다.
  >
  > - **프로필 확인**: 이름, 휴대폰, 이메일, 타입(관리자 / 프로젝트 멤버), 역할(관리자 역할 또는 프로젝트별 역할)을 한눈에 확인합니다.
  > - **프로필 편집**: `편집`에서 **휴대폰 번호**를 수정합니다. (이름·이메일은 조회 전용입니다.)
  > - **비밀번호 변경**: `비밀번호 변경`에서 현재 비밀번호를 확인한 뒤 새 비밀번호로 변경합니다.

- **배치 위치 제안**: 관리자·프로젝트 양쪽에서 진입하므로 **공통(계정) 영역**으로 신설하고 `README.md` 상단(소개 직후 또는 공통 섹션)에 등록하는 것을 권장합니다.
- **예상 분량**: 1페이지.

### P2. 인스턴스 상세 — `호스트 장치` / `AI 가속기` 탭 설명 보강 — **부분 누락**

- **UI 근거**: `cloudia-web/.../InstanceResourceSection.tsx`
  - `호스트 장치` 탭: **조회 전용** 목록(장치 종류 / 공급업체 / 제품명).
  - `AI 가속기` 탭: 상세 화면에서 **추가·삭제 모달** 제공(`GuestAcceleratorSection.tsx`).
- **현재 매뉴얼 상태**: `docs/project/vm/instance/instance.md`는 매우 충실합니다(보안 유형 SEV 계열, 가상 CPU 피닝, 콘솔 접속, 라이브 리사이즈, 사용자 스크립트 등 완비). 다만 상세 화면 **`인스턴스 구성 요소` 목록**(389행)에 두 탭이 이름으로만 언급되고, `네트워크 인터페이스 탭`·`디스크 탭`·`사용자 스크립트 탭`처럼 **별도 소절이 없습니다.**
- **작성할 내용**: 다른 탭 소절과 **동일한 형식**으로, 각 탭에서 보이는 정보와 가능한 작업을 추가합니다.
  - `호스트 장치 탭`: 조회 항목(장치 종류·공급업체·제품명)과 "추가/삭제는 현재 화면에서 제공하지 않음"을 안내.
  - `AI 가속기 탭`: 상세에서 가속기를 추가·삭제하는 절차와 활성화 조건(인스턴스 상태 등).
- **유형**: 기존 페이지에 소절 2개 추가하는 수준의 가벼운 보강.

---

## 2. 정합성 / 하우스키핑 (저비용·고효용)

작성 전 **UI/i18n과 한 번 더 대조**한 뒤 반영합니다.

- **`README.md` 프로젝트 > 스토리지 목록에 `오브젝트 스토리지` 링크 누락**
  `docs/project/SUMMARY.md`와 `docs/project/storage/README.md`에는 있으나, 최상위 `README.md`의 스토리지 항목(이미지/블록/파일 시스템/스냅샷)에는 빠져 있습니다. → 링크 추가.
- **`README.md` `외부 IP` 링크 `.md` 확장자 누락**
  `[외부 IP](docs/project/network/external-ip/external-ip)` → 실제 파일명 확인 후 `.md` 보정.
- **용어 정합성 — `네트워크 접근 제어` vs `네트워크 ACL`**
  제품/네비는 `네트워크 ACL`을 사용하는데, 예제 가이드 12번 제목은 `네트워크 접근 제어`입니다. 명칭 통일(확인 후).
- **용어 확인 — `이미지 생성` vs `이미지 업로드`**
  이미지 화면의 실제 버튼 라벨을 `cloudia-web/src/i18n/ko.json`에서 확인하고 `image.md` 본문 용어를 UI와 정렬.

---

## 3. 범위 외 (명시적으로 제외)

- **L7 로드밸런서** (리스너 편집 / L7 타겟 그룹 / 런타임 모니터링): **별도 담당자 작성 중** → 제외.
- **NUMA 노드 튜닝**, **Huge Pages**: 백엔드(`GuestNumaNodeController` 등)에는 존재하지만 `cloudia-web` UI에 **노출되지 않음**. 사용자 가이드 비대상.
- 참고: 내부 갭 트래커 `docs/steering/manual-gap-ui.md`는 **git-ignored**(`.gitignore`에 `docs/steering/` 포함)이며 현재 **stale**합니다. 이 계획 문서가 최신 갭 분석을 대체하므로, 트래커는 별도로 정리(해소 항목 표시)하는 것을 권장합니다.

---

## 4. 담당 분배 · 진행 순서 제안

| 항목 | 우선순위 | 작업 성격 | 예상 분량 | 충돌 위험 |
|------|----------|-----------|-----------|-----------|
| P1. 내 정보(MyInfo) 신규 | 높음 | 새 페이지 + README 등록 | 1페이지 | 낮음(신규 파일) |
| P2. 인스턴스 탭 보강 | 중간 | 기존 `instance.md` 소절 추가 | 0.5페이지 | **중간** — `instance.md`를 다른 인원이 동시 편집 시 |
| H. 내비/용어 정리 | 낮음 | `README.md` 등 소수정 | 소 | 낮음 |

진행 순서: **P1 → P2 → H** 권장.
`instance.md`(P2)는 다른 인원이 동시에 손댈 가능성이 있으므로, 착수 전 담당자 확인 후 진행합니다.

---

## 5. 작성 후 검증 방법

- **UI 1:1 대조**: 신규·수정한 각 페이지를 실제 화면과 비교해 라벨·필드·버튼 활성화 조건이 일치하는지 확인합니다.
- **링크 검증**: `README.md`·각 `SUMMARY.md`의 링크를 클릭해 깨진 링크가 없는지 확인합니다.
- **용어 대조**: 화면 라벨은 `cloudia-web/src/i18n/ko.json`을 기준으로 맞춥니다.

---

## 부록. 트래커 항목 검증 결과 (재작업 방지용)

`docs/steering/manual-gap-ui.md`의 각 후보 항목을 **현재 매뉴얼 기준으로 재확인**한 결과입니다. (✅ 이미 반영됨 / ⚠️ 보강 필요 / ❌ 누락)

| 영역 | 항목 | 상태 | 근거 |
|------|------|------|------|
| VM | GPU/NPU 가속기, CPU 피닝, 콘솔(VNC), 라이브 리사이즈, 사용자 스크립트, CD-ROM/ISO | ✅ | `instance.md` (콘솔 393–416, 라이브 329–375, 보안유형 546–574, 피닝 607–615, 사용자스크립트 462–468) |
| VM | 호스트 장치 패스스루 탭, AI 가속기 상세 탭 | ⚠️ | `instance.md` 389행에 탭 언급은 있으나 소절 없음 → **P2** |
| VM | 시작 템플릿 Managed VPC 옵션 | ✅ | `launch-template.md` |
| 네트워크 | NACL 편집, 네트워크 토폴로지, 서브넷 DNS, 플로팅 IP 타겟 유형 | ✅ | `nacl.md`, `network-topology.md`, `subnet.md`, `floating-ip.md` |
| 네트워크 | L7 로드밸런서 / L7 타겟 그룹 | — | **범위 외(별도 담당)** |
| 스토리지 | 오브젝트 스토리지(버킷·자격증명 갱신), VirtioFS, 블록 설명 수정 | ✅ | `object-storage.md`, `file-system.md`, `block.md` |
| 쿠버네티스 | CA 로테이션/재설정, kubeconfig 추출, 워커 노드그룹/모니터링/리소스 탭, 스토리지 | ✅ | `cluster.md` (49–62, 65–187, 139–168) |
| 물리 머신 | BMC 전원·섀시·SEL·센서, 하드웨어/로그/CPU 탭, 컴퓨트 노드 오버커밋 | ✅ | `machine.md` (전원 37–57, 섀시 233–249, SEL 253–264, 센서 270–302, 하드웨어 185–197, 로그 214–229), `compute-node.md` 46–66 |
| 접근 관리 | 내 정보(MyInfo) | ❌ | 페이지 없음 → **P1** |
| 접근 관리 | 멤버 역할/권한 세분 수정 | ✅ | `member.md` 73–86 |
| 환경설정 | 알림 SMTP+웹훅, LDAP 매퍼 | ✅ | `alarm-setting.md` 7–150, `ldap.md` 42–150 |
| (제외) | NUMA 튜닝, Huge Pages | — | **UI 미노출 → 비대상** |
