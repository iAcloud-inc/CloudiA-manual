# 이벤트(사용자)

**이벤트** 페이지는 특정 프로젝트에서 발생하는 다양한 이벤트 로그를 시간순으로 보여주는 모니터링 화면입니다.  
여기서 **이벤트**란 시스템 동작(예: 인스턴스 생성/삭제)과 같은 중요한 사건을 기록한 것입니다.  
프로젝트에 속한 사용자는 이 페이지를 통해 해당 프로젝트에서 일어난 작업이나 변화를 한눈에 파악하고, 필요한 경우 세부 내용을 확인할 수 있습니다.  
이벤트는 발생 시각과 레벨, 유형, 관련 프로젝트, 메세지 등의 정보를 포함하며 **읽기 전용**으로 제공됩니다.  

---

## 이벤트 목록

**이벤트 페이지**에서는 최근 24내 프로젝트 내에서 발생한 모든 이벤트를 리스트 형식으로 확인할 수 있습니다.

검색 결과는 표 형태로 표시되며, **최근 발생 이벤트가 상단에 정렬**됩니다.

 
| 컬럼명 | 설명 |
|---------|------|
| **생성 일시** | 이벤트 발생 시간 (로컬 시간 기준), 클릭 시 최신순/과거순 정렬 가능 |
| **이벤트 레벨** | INFO / WARN / ERROR / CRITICAL 수준 표시 |
| **이벤트명** | 이벤트의 유형 (예: 로그인, 로그아웃, 인스턴스 생성 등) |
| **프로젝트** | 이벤트가 발생한 프로젝트명 |
| **메세지** | 이벤트 상세 설명 (사용자, 리소스명, 오류 메시지 등 포함) |


### 이벤트 레벨

이벤트 레벨은 이벤트의 심각도를 나타내는 지표입니다.

| 이벤트 수준 | 설명 |
|--------------|------------|
| **INFO** | 일반 정보성 이벤트 |
| **WARN** | 경고성 이벤트 |
| **ERROR** | 오류관련 이벤트 |
| **CRITICAL** | 시스템에 영향을 줄 수 있는 이벤트 |


### 이벤트 검색

이벤트가 많을 경우 필터링 기능을 통해 원하는 데이터를 빠르게 조회할 수 있습니다. 필터링 조건은 아래와 같습니다.
| 필터 유형 | 설명 |
|------------|------|
| **검색어** | 키워드 입력으로 이벤트를 검색할 수 있음. “이벤트명” 또는 “사용자명” 기준 선택 가능 |
| **이벤트 레벨(Level)** | INFO / WARN / ERROR / CRITICAL 또는 “모든 이벤트 레벨” 중 선택하여 심각도별 필터링 가능 |
| **날짜 범위** | 캘린더 아이콘 클릭 후 시작일·종료일 지정 가능 (기본 24시간) |

여러 조건을 동시에 설정할 수 있으며, “검색” 버튼을 눌러 필터를 적용하여 조회할 수 있습니다.  

> 💡 날짜 범위 변경은 즉시 적용됩니다.
> 💡 조건에 일치하는 이벤트가 없는 경우 “리스트 데이터가 존재하지 않습니다.” 메세지가 표시됩니다.  

---




## 정의된 이벤트 리스트

|설명|설명 문구|이벤트 레벨
|------|------|------|
| 프로젝트 생성|{username}이 프로젝트({(name)\|id: (projectId)})를 생성했습니다.|INFO |
| 프로젝트 생성 실패|{username}이 프로젝트 생성을 실패했습니다.|ERROR |
| 프로젝트 편집|{username}이 프로젝트({(name)\|id: (projectId)})를 편집했습니다.|INFO |
| 프로젝트 편집 실패|{username}이 프로젝트 편집을 실패했습니다.|ERROR |
| 프로젝트 삭제|{username}이 프로젝트({(name)\|id: (projectId)})를 삭제했습니다.|INFO |
| 프로젝트 삭제 실패|{username}이 프로젝트를 삭제 실패했습니다.|ERROR |
| VPC 생성|{username}이 VPC({(name)})를 생성했습니다.|INFO |
| VPC 생성 실패|{username}이 VPC 생성을 실패했습니다.|ERROR |
| VPC 편집|{username}이 VPC({(name)\|id: (resourceId)})를 편집했습니다.|INFO |
| VPC 편집 실패|{username}이 VPC 편집을 실패했습니다.|ERROR |
| VPC 삭제|{username}이 VPC({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| VPC 삭제 실패|{username}이 VPC 삭제를 실패했습니다.|ERROR |
| 서브넷 생성|{username}이 서브넷({(name)})을 생성했습니다.|INFO |
| 서브넷 생성 실패|{username}이 서브넷 생성을 실패했습니다.|ERROR |
| 서브넷 편집|{username}이 서브넷({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 서브넷 편집 실패|{username}이 서브넷 편집을 실패했습니다.|ERROR |
| 서브넷 삭제|{username}이 서브넷({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 서브넷 삭제 실패|{username}이 서브넷 삭제를 실패했습니다.|ERROR |
| 외부 IP 생성|{username}이 외부IP({(pubAddr)})를 생성했습니다.|INFO |
| 외부 IP 생성 실패|{username}이 외부IP 생성을 실패했습니다.|ERROR |
| 외부 IP 편집|{username}이 외부IP({(pubAddr)\|id: (resourceId)})를 편집했습니다.|INFO |
| 외부 IP 편집 실패|{username}이 외부IP 편집을 실패했습니다.|ERROR |
| 외부 IP 삭제|{username}이 외부IP({(pubAddr)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 외부 IP 삭제 실패|{username}이 외부IP 삭제를 실패했습니다.|ERROR |
| 플로팅 IP 생성|{username}이 플로팅IP({(name)})를 생성했습니다.|INFO |
| 플로팅 IP 생성 실패|{username}이 플로팅IP 생성을 실패했습니다.|ERROR |
| 플로팅 IP 편집|{username}이 플로팅IP({(name)\|id: (resourceId)})를 편집했습니다.|INFO |
| 플로팅 IP 편집 실패|{username}이 플로팅IP 편집을 실패했습니다.|ERROR |
| 플로팅 IP 삭제|{username}이 플로팅IP({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 플로팅 IP 삭제 실패|{username}이 플로팅IP 삭제를 실패했습니다.|ERROR |
| 플로팅 IP 리소스 연결|{username}이 플로팅IP({(name)\|id: (resourceId)})에 리소스를 연결했습니다.|INFO |
| 플로팅 IP 리소스 연결 실패|{username}이 플로팅IP에 리소스 연결을 실패했습니다.|ERROR |
| 플로팅 IP 리소스 연결 해제|{username}이 플로팅IP({(name)\|id: (resourceId)})에 리소스를 연결 해제했습니다.|INFO |
| 플로팅 IP 리소스 연결 해제 실패|{username}이 플로팅IP에 리소스 연결 해제를 실패했습니다.|ERROR |
| 로드 밸런서 생성|{username}이 로드 밸런서({(name)})를 생성했습니다.|INFO |
| 로드 밸런서 생성 실패|{username}이 로드 밸런서 생성을 실패했습니다.|ERROR |
| 로드 밸런서 편집|{username}이 로드 밸런서({(name)\|id: (resourceId)})를 편집했습니다.|INFO |
| 로드 밸런서 편집 실패|{username}이 로드 밸런서 편집을 실패했습니다.|ERROR |
| 로드 밸런서 삭제|{username}이 로드 밸런서({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 로드 밸런서 삭제 실패|{username}이 로드 밸런서 삭제를 실패했습니다.|ERROR |
| 로드 밸런서 리스너 변경|{username}이 로드 밸런서({(name)\|id: (resourceId)})의 리스너를 변경했습니다.|INFO |
| 로드 밸런서 리스너 변경 실패|{username}이 로드 밸런서의 리스너 변경을 실패했습니다.|ERROR |
| 타겟 그룹 생성|{username}이 타겟 그룹({(name)})을 생성했습니다.|INFO |
| 타겟 그룹 생성 실패|{username}이 타겟 그룹 생성을 실패했습니다.|ERROR |
| 타겟 그룹 편집|{username}이 타겟 그룹({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 타겟 그룹 편집 실패|{username}이 타겟 그룹 편집을 실패했습니다.|ERROR |
| 타겟 그룹 삭제|{username}이 타겟 그룹({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 타겟 그룹 삭제 실패|{username}이 타겟 그룹 삭제를 실패했습니다.|ERROR |
| 타켓 그룹 타겟 변경|{username}이 타겟 그룹({(name)\|id: (resourceId)})의 타겟을 변경했습니다.|INFO |
| 타켓 그룹 타겟 변경 실패|{username}이 타겟 그룹의 타겟 변경을 실패했습니다.|ERROR |
| 네트워크 접근 제어 생성|{username}이 네트워크 접근제어({(name)})를 생성했습니다.|INFO |
| 네트워크 접근 제어 생성 실패|{username}이 네트워크 접근제어 생성을 실패했습니다.|ERROR |
| 네트워크 접근 제어 삭제|{username}이 네트워크 접근제어({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 네트워크 접근 제어 삭제 실패|{username}이 네트워크 접근제어 삭제를 실패했습니다.|ERROR |
| 보안그룹 생성|{username}이 보안그룹({(name)})을 생성했습니다.|INFO |
| 보안그룹 생성 실패|{username}이 보안그룹 생성을 실패했습니다.|ERROR |
| 보안그룹 편집|{username}이 보안그룹({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 보안그룹 편집 실패|{username}이 보안그룹 편집을 실패했습니다.|ERROR |
| 보안그룹 삭제|{username}이 보안그룹({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 보안그룹 삭제 실패|{username}이 보안그룹 삭제를 실패했습니다.|ERROR |
| 이미지 생성|{username}이 이미지를 생성했습니다.|INFO |
| 이미지 생성 실패|{username}이 이미지 생성을 실패했습니다.|ERROR |
| 이미지 삭제|{username}이 이미지({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 이미지 삭제 실패|{username}이 이미지 삭제를 실패했습니다.|ERROR |
| 블록 생성|{username}이 블록({(name)})을 생성했습니다.|INFO |
| 블록 생성 실패|{username}이 블록 생성을 실패했습니다.|ERROR |
| 블록 삭제|{username}이 블록({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 블록 삭제 실패|{username}이 블록 삭제를 실패했습니다.|ERROR |
| 파일시스템 생성|{username}이 파일시스템({(name)})을 생성했습니다.|INFO |
| 파일시스템 생성 실패|{username}이 파일시스템 생성을 실패했습니다.|ERROR |
| 파일시스템 삭제|{username}이 파일시스템({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 파일시스템 삭제 실패|{username}이 파일시스템 삭제를 실패했습니다.|ERROR |
| 인스턴스 생성|{username}이 인스턴스({(name)\|id: (resourceId)})를 생성했습니다.|INFO |
| 인스턴스 생성 실패|{username}이 인스턴스 생성을 실패했습니다.|ERROR |
| 인스턴스 삭제|{username}이 인스턴스({(name)\|id: (resourceId)})를 삭제했습니다.|INFO |
| 인스턴스 삭제 실패|{username}이 인스턴스 삭제를 실패했습니다.|ERROR |
| 인스턴스 편집|{username}이 인스턴스({(name)\|id: (resourceId)})를 편집했습니다.|INFO |
| 인스턴스 편집 실패|{username}이 인스턴스 편집을 실패했습니다.|ERROR |
| 인스턴스 실행|{username}이 인스턴스({(name)\|id: (resourceId)})를 실행했습니다.|INFO |
| 인스턴스 실행 실패|{username}이 인스턴스 실행을 실패했습니다.|ERROR |
| 인스턴스 종료|{username}이 인스턴스({(name)\|id: (resourceId)})를 종료했습니다.|INFO |
| 인스턴스 종료 실패|{username}이 인스턴스 종료를 실패했습니다.|ERROR |
| 인스턴스 재부팅|{username}이 인스턴스({(name)\|id: (resourceId)})를 재부팅했습니다.|INFO |
| 인스턴스 재부팅 실패|{username}이 인스턴스 재부팅을 실패했습니다.|ERROR |
| 인스턴스 라이브 마이그레이션|{username}이 인스턴스({(name)\|id: (resourceId)})를 라이브 마이그레이션 했습니다.|INFO |
| 인스턴스 라이브 마이그레이션 실패|{username}이 인스턴스 라이브 마이그레이션을 실패했습니다.|ERROR |
| 인스턴스 스냅샷 생성|{username}이 스냅샷({name})을 생성했습니다.|INFO |
| 인스턴스 스냅샷 생성 실패|{username}이 스냅샷 생성을 실패했습니다.|ERROR |
| 스냅샷 삭제|{username}이 스냅샷({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 스냅샷 삭제 실패|{username}이 스냅샷 삭제를 실패했습니다.|ERROR |
| 스냅샷 편집|{username}이 스냅샷({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 스냅샷 편집 실패|{username}이 스냅샷 편집을 실패했습니다.|ERROR |
| 스냅샷 롤백|{username}이 스냅샷({(name)\|id: (resourceId)})으로 롤백했습니다.|INFO |
| 스냅샷 롤백 실패|{username}이 스냅샷으로 롤백을 실패했습니다.|ERROR |
| 오토 스케일링 그룹 생성|{username}이 오토 스케일링 그룹({name})을 생성했습니다.|INFO |
| 오토 스케일링 그룹 생성 실패|{username}이 오토 스케일링 그룹 생성을 실패했습니다.|ERROR |
| 오토 스케일링 그룹 삭제|{username}이 오토 스케일링 그룹({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 오토 스케일링 그룹 삭제 실패|{username}이 오토 스케일링 그룹 삭제를 실패했습니다.|ERROR |
| 시작 템플릿 생성|{username}이 시작 템플릿({name})을 생성했습니다.|INFO |
| 시작 템플릿 생성 실패|{username}이 시작 템플릿 생성을 실패했습니다.|ERROR |
| 시작 템플릿 편집|{username}이 시작 템플릿({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 시작 템플릿 편집 실패|{username}이 시작 템플릿 편집을 실패했습니다.|ERROR |
| 시작 템플릿 삭제|{username}이 시작 템플릿({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 시작 템플릿 삭제 실패|{username}이 시작 템플릿 삭제를 실패했습니다.|ERROR |
| 시작 템플릿으로 인스턴스 생성|{username}이 시작 템플릿({(name)\|id: (resourceId)})으로 인스턴스를 생성했습니다.|INFO |
| 시작 템플릿으로 인스턴스 생성 실패|{username}이 시작 템플릿으로 인스턴스 생성을 실패했습니다.|ERROR |
| 선호도 그룹 생성|{username}이 선호도 그룹({name})을 생성했습니다.|INFO |
| 선호도 그룹 생성 실패|{username}이 선호도 그룹 생성을 실패했습니다.|ERROR |
| 선호도 그룹 삭제|{username}이 선호도 그룹({(name)\|id: (resourceId)})을 삭제했습니다.|INFO |
| 선호도 그룹 삭제 실패|{username}이 선호도 그룹 삭제를 실패했습니다.|ERROR |
| 선호도 그룹 편집|{username}이 선호도 그룹({(name)\|id: (resourceId)})을 편집했습니다.|INFO |
| 선호도 그룹 편집 실패|{username}이 선호도 그룹 편집을 실패했습니다.|ERROR |
| 사용자 역할 수정|{targetName}의 역할이 변경됐습니다.|INFO |
| 사용자 역할 수정 실패|{targetName}의 역할 변경을 실패했습니다.|ERROR |
| 역할 생성|{username}이 역할({name})을 생성했습니다.|INFO |
| 역할 생성 실패|{username}이 역할 생성을 실패했습니다.|ERROR |
| 역할 삭제|{username}이 역할({name})을 삭제했습니다.|INFO |
| 역할 삭제 실패|{username}이 역할 삭제를 실패했습니다.|ERROR |
| 역할 편집|{username}이 역할({name})을 편집했습니다.|INFO |
| 역할 편집 실패|{username}이 역할 편집을 실패했습니다.|ERROR |
| 역할 권한 변경|{username}이 역할({name})의 권한을 변경했습니다.|INFO |
| 패스워드 정책 변경|{username}이 패스워드 정책을 변경했습니다.|INFO |
| 패스워드 정책 변경 실패|{username}이 패스워드 정책 변경을 실패했습니다.|ERROR |
| 계정 잠금 정책 변경|{username}이 계정 잠금 정책을 변경했습니다.|INFO |
| 계정 잠금 정책 변경 실패|{username}이 계정 잠금 정책 변경을 실패했습니다.|error |
| 메일 발송 설정|{username}이 메일 발송 설정을 변경했습니다.|INFO |
| 메일 발송 설정 실패|{username}이 메일 발송 설정을 실패했습니다.|ERROR |
| 메일 발송 설정 삭제|{username}이 메일 발송 설정을 삭제했습니다.|INFO |
| 메일 발송 설정 삭제 실패|{username}이 메일 발송 설정 삭제를 실패했습니다.|ERROR |
| 임시 비활성화 해제|{username}이 사용자의 임시 비활성화를 해제했습니다.|INFO |
| 임시 비활성화 해제 실패|{username}이 사용자의 임시 비활성화 해제를 실패했습니다.|ERROR |
| 임시 비활성화 일괄 해제|{username}이 사용자의 임시 비활성화를 모두 해제했습니다.|INFO |
| 임시 비활성화 일괄 해제 실패|{username}이 사용자의 임시 비활성화 모두 해제를 실패했습니다.|ERROR |
| 내 정보 수정|{username}이 내 정보를 수정했습니다.|INFO |
| 내 정보 수정 실패|{username}이 내 정보 수정을 실패했습니다.|ERROR |
| 로그인|{username}이 로그인 했습니다.|INFO |
| 로그인 실패|{username}이 로그인 실패했습니다.|WARN |
| 로그아웃|{username}이 로그아웃 했습니다.|INFO |
| 임시 비활성화|{username}이 임시 비활성화 됐습니다.|WARN |
| CPU 사용량 과다|노드({hostname})의 CPU 사용량이 {cpuUsageThreshold}%를 초과했습니다.|CRITICAL |
| MEMORY 사용량 과다|노드({hostname})의 MEMORY 사용량이 {memoryUsageThreshold}%를 초과했습니다.|CRITICAL |
| 노드 온라인|노드({hostname})가 연결 되었습니다.|INFO |
| 노드 오프라인|노드({hostname})의 연결이 끊겼습니다.|CRITICAL |

